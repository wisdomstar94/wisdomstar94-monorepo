import { cn } from '@/utils';

export function Logo() {
  return (
    <div
      className={cn(
        'inline-flex items-center justify-center relative flex-col font-paperlogy-800-extra-bold text-slate-700'
      )}
    >
      <div>@wisdomstar94</div>
    </div>
  );
}
